# 2026-05-20

## 13:08 - Country flag fix
- Added flag emojis to ALL country selector translation values in ALL 48 language files (24 src + 24 dist)
- Fixed 6 HTML files (3 src + 3 dist) — moved `data-i18n` from child `<span>` to parent `<option>` for China/Japan/South Korea/India/Saudi Arabia/Taiwan, enabling custom-select with `data-i18n` propagation to `.cs-item`
- Now all 28 countries show flags in the dropdown ✅
- Root cause: custom-select `_buildOptionItemsHTML` propagates `data-i18n` from `<option>` to `.cs-item`, but for child `<span data-i18n>` pattern, the `<option>` had no `data-i18n` itself → `.cs-item` didn't get the attibute → i18n system couldn't update its text

## 13:14 - Menu duplication fix
- Removed "查看全部行业场景" (view all industry scenarios) from applications dropdown:
  - `applications-dropdown.js` (src + dist): removed `viewAllItem` from `renderDropdown()`, removed `viewAllHtml` from `buildPopupContent()`
  - `slide-menu.js` (src + dist): removed the mobile menu "查看全部行业场景" link in applications section
  - Item was duplicate of "应用场景总览" — both link to `/applications/`

## 13:14 - Cases Data Cards mobile fix
- Changed all 8 case sub-pages (bangkok, cebu, hanoi, hcmc, jakarta, kl, manila, surabaya) × 3 variants (pc/mobile/tablet) × 2 (src + dist) = 48 files
- PC variant: `grid-cols-4` → `grid-cols-1 sm:grid-cols-2 lg:grid-cols-4`
- Tablet variant: `grid-cols-2` → `grid-cols-1 sm:grid-cols-2 lg:grid-cols-2`
- Cards now display at full width (287px) instead of cramped 62.75px on 375px viewport
